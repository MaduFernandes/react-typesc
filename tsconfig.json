{
    "compilerOptions": {
      "target": "es6",                          // Especifica o padrão de saída do JavaScript (ES6/ES2015)
      "module": "commonjs",                     // Especifica o sistema de módulos
      "lib": ["es6", "dom"],                    // Inclui bibliotecas padrão ES6 e DOM
      "sourceMap": true,                        // Gera mapas de origem para depuração
      "declaration": true,                      // Gera arquivos de declaração (.d.ts)
      "outDir": "./dist",                       // Diretório de saída para os arquivos compilados
      "rootDir": "./src",                       // Diretório raiz dos arquivos de entrada
      "baseUrl": "./src",                      // Define a base URL para resolução de módulos
      "strict": true,                           // Ativa todas as verificações de tipo estritas
      "noImplicitAny": true,                    // Gera um erro se uma expressão tem um tipo 'any' implícito
      "strictNullChecks": true,                 // Habilita checagem rigorosa de null e undefined
      "strictFunctionTypes": true,              // Habilita checagem rigorosa de tipos de funções
      "strictPropertyInitialization": true,     // Garante que as propriedades sejam inicializadas
      "noImplicitThis": true,                   // Gera um erro se 'this' é implicitamente do tipo 'any'
      "noUnusedParameters": true,               // Gera um erro se parâmetros não utilizados são encontrados
      "noUnusedLocals": true,                   // Gera um erro se variáveis locais não utilizadas são encontradas
      "esModuleInterop": true,                  // Habilita interoperabilidade entre módulos CommonJS e ES
      "skipLibCheck": true,                     // Ignora checagem de tipo dos arquivos de definição (*.d.ts)
      "jsx": "react-jsx"                        // Configura o JSX para a nova transformação JSX (React 17+)
    },
    "include": [
      "src/**/*"                               // Inclui todos os arquivos TypeScript no diretório 'src'
    ],
    "exclude": [
      "node_modules",                        // Exclui arquivos de teste
    ]
  }
  